name: Deploy Pages – Fresh Trigger

on:
  push:
    paths:
      - '**.html'
      - '**.yml'
      - '**.md'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v3

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v2
      - name: Temporary patch – install upload-artifact
        uses: actions/upload-artifact@v3
        with:
          name: placeholder
          path: ./index.html

      - name: Deploy to GitHub Pages
        id: deployment

 - name: Temporary patch – install upload-artifact
        uses: actions/upload-artifact@v3
        with:
          name: placeholder
          path: ./index.html        uses: actions/deploy-pages@v2

